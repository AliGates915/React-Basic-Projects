import React from "react";

const UserContext =  React.createContext()

/* React.createContext() is a function provided by the React library in JavaScript. 
It is used to create a context object, which can then be used to share values 
(such as state or functions) between components in a React application without 
having to pass the props manually at every level of the component tree.
React.createContext() is a function provided by the React library in JavaScript. 
It is used to create a context object, which can then be used to share values 
(such as state or functions) between components in a React application without 
having to pass the props manually at every level of the component tree.
 */

export default UserContext;